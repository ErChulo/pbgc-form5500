{
  "name": "pbgc-form5500",
  "version": "0.7.0",
  "private": true,
  "description": "Single-file browser ingestor for Form 5500 PDFs and EFAST CSV exports",
  "scripts": {
    "build": "node scripts/build.js",
    "serve": "node scripts/serve.js",
    "test": "node --test tests/*.test.js"
  }
}
